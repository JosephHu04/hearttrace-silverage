# 协作与合并规则

## 分支规则

- 禁止直接向 `main` 推送；每项工作从最新 `main` 创建独立分支。
- 命名格式：`feature/<scope>-<topic>`、`fix/<scope>-<topic>` 或 `chore/<topic>`。
- 一个分支只处理一个主题。不要在同一 PR 同时修改老人端、家属端和接口协议，除非它们属于同一个可说明的联调变更。
- 合并前通过 Pull Request；CI 必须通过，且至少获得一名组员批准。

## 避免冲突

1. 开工前在群内登记“分支名、负责目录、计划修改的接口”。
2. 页面开发优先使用 Mock；跨端数据需求先改 `docs/api-contract.md`，确认字段后再各自开发。
3. 不复制风险等级、授权判断和量表计分逻辑到前端；这些只由后端实现。
4. 需要改别人负责的目录时，先在 PR 中 @ 负责人；不要顺手格式化或重构无关文件。
5. 合并前同步最新 `main`。出现冲突时由改动该业务的人解决，并重新跑 CI；不要用“保留任一方全部内容”来消除冲突。

## 审查重点

- 授权、隐私、风险与紧急事件是否仍由服务端控制。
- 心情打卡、标准量表和危机事件是否仍然分离。
- 是否破坏其他端已冻结的接口。
- 是否包含真实敏感数据、密钥或不必要的依赖。

详细的心理筛查和危机边界见 [筛查标准](docs/screening-standard.md)。
